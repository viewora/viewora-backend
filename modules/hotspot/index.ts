// Hotspot module — stub for V1
// Handles CRUD for hotspots stored in property_360_settings.hotspots_json.
// Routes mounted under /spaces/:id/hotspots.

import type { FastifyInstance } from 'fastify'

export default async function hotspotModule(fastify: FastifyInstance) {
  // Placeholder — hotspot routes registered here in V1
}
