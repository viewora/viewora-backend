// Scene module — stub for V1
// Handles panorama/gallery/car_spin media records per space.
// Routes will be registered here and mounted in src/index.ts under /spaces/:id/scenes.

import type { FastifyInstance } from 'fastify'

export default async function sceneModule(fastify: FastifyInstance) {
  // Placeholder — scene sub-routes registered here in V1
}
