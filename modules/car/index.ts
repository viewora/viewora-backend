// Car spin module — stub for V1
// Handles frame-sequence upload and config for automotive space type.
// Routes mounted under /spaces/:id/car-spin.

import type { FastifyInstance } from 'fastify'

export default async function carModule(fastify: FastifyInstance) {
  // Placeholder — car-spin routes registered here in V1
}
